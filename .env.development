# Environnement « dev » (vite --mode development). Aucun secret ici : tout ce qui
# commence par VITE_ est embarqué dans le bundle. Le DSN Sentry est fourni
# par la CI (secret GitHub) ou par .env.local.
VITE_APP_ENV=dev
VITE_API_BASE_URL=
