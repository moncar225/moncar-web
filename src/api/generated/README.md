/**
 * Espace réservé aux clients TypeScript générés depuis le contrat OpenAPI
 * officiel (Richard — J5-J6 du Sprint 1).
 *
 * RÈGLE CONTRACT-FIRST :
 * - AUCUN fichier ne doit être écrit ou édité à la main dans ce dossier.
 * - Toute signature de service proviendra exclusivement de l'openapi.yaml
 *   via un générateur (ex. openapi-typescript, openapi-generator, Orval…).
 * - Si un endpoint n'existe pas dans le contrat, il n'existe pas ici.
 *
 * Lorsque le contrat OpenAPI sera disponible :
 *   1. déplacer openapi.yaml (ou pointer vers son URL publique) ;
 *   2. ajouter un script de génération au package.json ;
 *   3. ré-exporter le client généré pour que les features l'utilisent via
 *      `@/api/client` en lui injectant `HttpClient` de ce même package.
 */
